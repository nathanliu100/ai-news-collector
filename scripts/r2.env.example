# Cloudflare R2 凭证（复制为 r2.env 后填写，r2.env 已被 .gitignore 忽略，不会进仓库）
# 获取方式见下方说明
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
R2_BUCKET=

# ── 如何拿到这三个值 ──
# 1. CLOUDFLARE_ACCOUNT_ID
#    Cloudflare 控制台 → 右侧栏 "Account ID"，或 R2 概览页 URL 里那串 hex
#
# 2. R2_BUCKET
#    就是你存视频的那个桶名（公开域名 pub-51163ae...r2.dev 对应的桶）
#    R2 → Overview → 桶列表里看名字
#
# 3. CLOUDFLARE_API_TOKEN（关键，scoped 最小权限）
#    Cloudflare 控制台 → R2 → 右上 "Manage R2 API Tokens"（或
#    My Profile → API Tokens → Create Token → 找 R2 模板）
#    → Create API Token
#    → Permissions 选 "Object Read & Write"
#    → Specify bucket: 只选你那一个桶
#    → 创建后复制 "Token value"（只显示一次）粘到上面
